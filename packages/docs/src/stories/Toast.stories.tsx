import type { Meta, StoryObj } from '@storybook/react'
import { Toast, ToastProps } from '@rauleffting-ds/react'

export default {
  title: 'Form/Toast',
  component: Toast,
} as Meta<ToastProps>

export const Primary: StoryObj<ToastProps> = {}
